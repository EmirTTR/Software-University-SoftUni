using System;
//////////////
namespace FriquencyOfElement
{
    class Program
    {
        static void Main(string[] args)
        {
            int num = int.Parse(Console.ReadLine());
            int[] elements = new int[num];
            int count = 0;

            for (int i = 0; i < num; i++)
            {
                elements[i] = int.Parse(Console.ReadLine());

                if ()
                {
                    
                }
            }
        }
    }
}
